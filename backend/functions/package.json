{
  "name": "prism-functions",
  "private": true,
  "engines": { "node": "18" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p .",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "stripe": "^16.0.0",
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.5.0",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
